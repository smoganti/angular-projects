import { Component } from '@angular/core';

//naming convention as the server class with an inside component file

//decorator to enhance the classes or components
@Component({
    selector: 'app-server',
    templateUrl: './server.component.html'
})
export class ServerComponent {
    
}